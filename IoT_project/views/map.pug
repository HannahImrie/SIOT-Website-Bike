//views/layout.pug
extends layout.pug
block content
  #map
  #leg_title 
    span#leg_items Map Legend
  #leg
  	result
      input(id=layer.name)(type='checkbox', checked)
	  span#leg_items #{layer.name}
      br